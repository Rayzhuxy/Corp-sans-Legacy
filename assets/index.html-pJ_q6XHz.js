import{_ as a,e as o,f as t,o as s}from"./app-DNQBHjly.js";const c="/assets/image-20220209155637692-CQ5xEr9Y.png",r="/assets/image-20220209162214117-_jvLnOSO.png",d="/assets/image-20220514154426238-BoGI-71s.png",i="/assets/image-20220514155016392-DhzIaYBZ.png",p="/assets/image-20220209174417698-BjIa7lvb.png",n="/assets/image-20220517103915205-BnszQY1d.png",l="/assets/image-20220517112942391-B8wB4C1u.png",h="/assets/image-20220514163402006-BP2UpDVV.png",m="/assets/image-20220517151708835-BJiz1UNR.png",u="/assets/image-20220517164038415-abvmcf89.png",g="/assets/image-20220517172031273-9CGKClop.png",_="/assets/image-20220517152214689-BdD_K9mR.png",b={};function f(q,e){return s(),o("div",null,e[0]||(e[0]=[t('<blockquote><p>STL 就是泛型编程的最好示例。</p></blockquote><p>通过泛型编程使用 C++ 标准库的 6 大部件。</p><p>6 大部件 (Components) ：</p><ul><li>容器 Containers</li><li>分配器 Allocators ：容器通常都有默认的分配器，因此不需要使用者操心。</li><li>算法 Algorithms</li><li>迭代器 Iterators</li><li>适配器 Adapters</li><li>仿函数 Functors</li></ul><p><img src="'+c+'" alt="image-20220209155637692"></p><h3 id="容器-结构与分类" tabindex="-1"><a class="header-anchor" href="#容器-结构与分类"><span>容器 - 结构与分类</span></a></h3><ul><li>顺序容器：数组或链表实现</li><li>关联容器 <ul><li>有序：红黑树实现</li><li>无序：哈希表实现（一组桶） <em>C++11</em></li></ul></li></ul><p><img src="'+r+'" alt="image-20220209162214117"></p><blockquote><p>红框标注的为 C++11 新增容器，其中 array 是将原本的语言特显变为 array class</p><p>vector 的扩充通过“分配器”实现，分配器完成底层的内存分配工作</p></blockquote><h3 id="oop-vs-gp" tabindex="-1"><a class="header-anchor" href="#oop-vs-gp"><span>OOP vs. GP</span></a></h3><p>面向对象（OOP）企图将 datas 和 methods 关联在一起； 泛型编程（GP）则将 datas 和 methods 分开来。</p><p>泛型编程使得容器和算法可以各自闭门造车，它们之间的接口是 Iterators ，算法通过迭代器确定操作范围，并通过迭代器取用容器的元素。</p><h3 id="分配器" tabindex="-1"><a class="header-anchor" href="#分配器"><span>分配器</span></a></h3><p>TODO：内存管理</p><h3 id="容器结构与分类" tabindex="-1"><a class="header-anchor" href="#容器结构与分类"><span>容器结构与分类</span></a></h3><p><img src="'+d+'" alt="image-20220514154426238"></p><h3 id="list" tabindex="-1"><a class="header-anchor" href="#list"><span>list</span></a></h3><p>list 的迭代器对前置后后置自增运算符进行重载，如下图所示。注意其中使用 <code>*this</code> 时并没有先调用 <code>*</code> 运算符，而是直接将 <code>this</code> 作为参数调用相应的重载运算符 <code>=</code> 、<code>++</code>。</p><p><img src="'+i+'" alt="image-20220514155016392"></p><blockquote><p>后置 <code>++</code> 的返回类型不是引用类型。因为为了符合后置自增运算符的特性，返回必须是自增前的值，因此使用局部变量 <code>self tmp = *this;</code> 临时保存，当函数体结束，局部变量销毁，自然不能返回其引用值。</p></blockquote><h3 id="vector" tabindex="-1"><a class="header-anchor" href="#vector"><span>vector</span></a></h3><blockquote><p>参看 <em>《C++ Primer》</em> p317</p></blockquote><h3 id="deque" tabindex="-1"><a class="header-anchor" href="#deque"><span>deque</span></a></h3><p><strong>deque 对象是如何增长的？</strong></p><p>deque 对象中其实是一连串的指针，每一个指针指向一个 buffer 。buffer 具有固定的长度，能容纳 n 个元素，当 deque 向前或向后增长时首先填充 buffer 内空闲的内存，当 buffer 满了之后再分配新的 buffer。</p><p>而存储 deque 中的这一连串指针的正是 vector，它符合 vector 的增长规则，当存储指针的 vector 满了时，动态分配一块两倍大小的内存，并把原来的指针移（数据并不需要移动）到新内存的<strong>中间</strong>。</p><p><img src="'+p+'" alt="image-20220209174417698"></p><p>正如你所见，<code>deque</code> 容器内的数据并不是连续存放的，而是分段连续的。 <code>deque</code> 看起来“连续”的假象都是通过迭代器的精心设计来伪装的。此外 <code>deque</code> 底层支持随机访问，容器也提供了 <code>.at()</code> 和 <code>[]</code>。</p><blockquote><p><strong>stack 和 queue 实际上是 deque 容器的适配器</strong>，内涵一个 deque ，各种操作是对 deque 容器的操作的再次封装。</p><p>stack 和 queue 也可以选择其他容器作为底层结构（适配其他容器），但所选容器必须支持相对应的操作。<em>《C++ Primer》</em> P</p></blockquote><h3 id="map-和-set" tabindex="-1"><a class="header-anchor" href="#map-和-set"><span>map 和 set</span></a></h3><p>map 和 set 底层都通过红黑树来实现，标准库的实现通常有一个 rb_tree class，从某种意义上来说 map 和 set 也可以说是容器适配器，只不过红黑树的实现并不开放给用户使用。</p><p><strong>关于 <code>[]</code> 的效率</strong>：侯捷老师在课程中从源码分析，说明了 <code>[]</code> 操作会比直接使用 <code>.insert()</code> 方法更慢。但事实上，二者应该是差不多的。<code>[]</code> 操作符使用 <code>lower_bound()</code> （由于其只接收一个参数，这应该是 map 类自己实现的，而不是标准库的模板函数）查找 key 为 <code>__k</code> 的元素：</p><ul><li>如果没有找到，则插入。但此时调用 <code>.insert()</code> 时提供了应该插入的位置的迭代器 <code>__i</code>，因此 <code>.insert()</code> 函数可以直接插入并调整红黑树。如果没有 <code>__i</code> 直接使用 <code>.insert()</code> 插入，必然也需要先查找到插入位置，这个查找的效率和 <code>[]</code> 中使用的 <code>lower_bound()</code> 应该是相当的。（可以对比 PPT 中 135 页（使用 <code>.insert()</code>） 和 137 页（使用 <code>[]</code> ），耗时分别为 4812ms 和 4890ms）</li><li>如果找到了，则返回引用。直接通过迭代器 <code>__i</code> 返回即可，这和 <code>.at()</code> 的效率应该也是相当的。</li></ul><p><img src="'+n+'" alt="image-20220517103915205"></p><h3 id="unordered-map-和-unordered-set" tabindex="-1"><a class="header-anchor" href="#unordered-map-和-unordered-set"><span>unordered_map 和 unordered_set</span></a></h3><p>unordered_map 和 unordered_set 底层都通过 hashtable 实现，标准库的实现通常有一个 hashtable class。</p><p><strong>hash-code 如何产生？</strong> 标准库的实现对整数类型直接使用本身作为 hash-code，对应 <code>char*</code> 类型则通过每一个字符来计算产生。那小数以及 object 呢？</p><p><strong>hashtable 扩容</strong>：当哈希表中存储元素个数超过表长时（也可以是其他规则，大部分实现是这样做），将表长扩展到当前长度的二倍附近的质数（表长通常保持为质数）。<strong>然后将表中所有元素的 hash-code 对新表长取模，放置到新的正确的位置。</strong></p><p><img src="'+l+'" alt="image-20220517112942391"></p><h3 id="迭代器" tabindex="-1"><a class="header-anchor" href="#迭代器"><span>迭代器</span></a></h3><h4 id="关于迭代器和-iterator-traits-迭代器萃取器" tabindex="-1"><a class="header-anchor" href="#关于迭代器和-iterator-traits-迭代器萃取器"><span>关于迭代器和 iterator traits （迭代器萃取器）</span></a></h4><p>迭代器是泛化的指针，容器的迭代器通常是一个 class，<code>vector&lt;int&gt;::iterator it = vec.begin()</code> 即创建了一个迭代器对象，每个容器专属的迭代器 class 为对应的容器做了完美适配。</p><p>iterator traits 通过模板偏特化来区分 class iterators 和 non-class iterators，从而可以在泛型算法中通过迭代器对象（或指针）获取正确的元素类型等信息：</p><p><img src="'+h+'" alt="image-20220514163402006"></p><h4 id="迭代器的类别" tabindex="-1"><a class="header-anchor" href="#迭代器的类别"><span>迭代器的类别</span></a></h4><p><img src="'+m+'" alt="image-20220517151708835"></p><p>将迭代器类别使用 class 来实现，并使用继承体系，主要目的是为了便于模板算法针对不同类别迭代器实现相应的版本。</p><p>比如下图的 <code>distance()</code> 函数，针对继承链最底端的 <code>random_access_iterator_tag</code> 实现了一个版本，针对继承链最顶端的 <code>input_iterator_tag</code> 实现了一个版本，这样一来除了 <code>random_access_iterator_tag</code> 类型的迭代器，继承链中其他类迭代器（不包括 <code>output_iterator_tag</code> 将调用 <code>input_iterator_tag</code> 类版本，因为派生类对象向基类对象的转型。（具体的实现是，通过迭代器萃取器获取 <code>InputIterator</code> 类型的迭代器类型，并创建一个临时对象，函数将按照规则进行类型匹配）</p><p><img src="'+u+'" alt="image-20220517164038415"></p><blockquote><p>泛型算法看不见容器，对其一无所知；它需要的一切信息都必须从 iterators 取得，而 iterators（由容器提供） 必须能够回答算法的所有提问。算法通过迭代器种类来实现不同的操作，这事关效率。</p></blockquote><h3 id="一些算法" tabindex="-1"><a class="header-anchor" href="#一些算法"><span>一些算法</span></a></h3><ul><li>关联式容器有自己的成员函数版本：<code>.count()</code>、<code>.find()</code>，但关联式容器不能使用 <code>sort()</code> 。</li></ul><p>模板算法接收迭代器类型作为参数，没有限制哪些容器的迭代器类型才可以，但暗示了迭代器类型。如下图，<code>sort()</code> 只能使用随机访问的迭代器（使用其他迭代器也不会报错，但不能成功）。</p><p><img src="'+g+'" alt="image-20220517172031273"></p><ul><li>list 和 forward_list 容器有自带的 <code>.sort()</code></li></ul><h3 id="关于只有-typedef-的基类" tabindex="-1"><a class="header-anchor" href="#关于只有-typedef-的基类"><span>关于只有 typedef 的基类</span></a></h3><p>基类中只有一系列的 typedef，所有继承自该基类的派生类都具有这一系列 typedef，有助于在实现时有相同或类似的写法。比如：</p><p><img src="'+_+'" alt="image-20220517152214689"></p><blockquote><p>到第 30 讲</p></blockquote>',59)]))}const k=a(b,[["render",f],["__file","index.html.vue"]]),v=JSON.parse('{"path":"/HouJie/t790uhp4/","title":"STL 与泛型编程","lang":"zh-CN","frontmatter":{"title":"STL 与泛型编程","createTime":"2025/08/30 22:58:25","permalink":"/HouJie/t790uhp4/"},"headers":[],"readingTime":{"minutes":6.14,"words":1841},"git":{"updatedTime":1756567360000,"contributors":[{"name":"Rayzhuxy","username":"Rayzhuxy","email":"734400huachuan@gmail.com","commits":5,"avatar":"https://avatars.githubusercontent.com/Rayzhuxy?v=4","url":"https://github.com/Rayzhuxy"}]},"filePathRelative":"notes/HouJie/notes/STL 与泛型编程.md"}');export{k as comp,v as data};
